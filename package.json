{
  "name": "gestao-quatro5",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "api": "npm run dev --workspace=apps/api",
    "web": "npm run dev --workspace=apps/web",
    "build": "npm run prisma:generate --workspace=packages/database && npm run build --workspace=packages/database && npm run build --workspace=apps/api",
    "start": "npx prisma migrate deploy --schema=packages/database/prisma/schema.prisma && node apps/api/dist/index.js"
  }
}
